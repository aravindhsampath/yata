<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>com.aravindhsampath.yata.sync</string>
	</array>
	<!--
	  Bundled variable TTFs for the YATA redesign. Each file is a single
	  variable font supporting the full weight axis; SwiftUI Font.custom(...)
	  combined with .weight(...) maps onto the wght axis at render time.
	  Family names registered (resolved from each TTF's name table):
	    InterVariable.ttf  → "Inter Variable"  (body)
	    InterTight.ttf     → "Inter Tight"     (display)
	    JetBrainsMono.ttf  → "JetBrains Mono"  (mono labels)
	-->
	<key>UIAppFonts</key>
	<array>
		<string>InterVariable.ttf</string>
		<string>InterTight.ttf</string>
		<string>JetBrainsMono.ttf</string>
	</array>
</dict>
</plist>
